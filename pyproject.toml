[project]
name = "flow_planning"
version = "0.1.0"
description = "Flow Planning IsaacLab Project"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
  "lerobot>=0.5.1",
  "newton[examples]",
  "pre-commit>=4.6.0",
  "rerun-sdk>=0.26.2",
  "ty>=0.0.46",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff.lint]
select = ["E", "F", "I"]
